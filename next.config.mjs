/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'avatars.githubusercontent.com',
            'urj.org',
            'ceoconference.ro',
            'via.placeholder.com',
            'picsum.photos'
        ],
    }
};

export default nextConfig;
